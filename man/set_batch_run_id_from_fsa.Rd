% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fsa_metadata.R
\name{set_batch_run_id_from_fsa}
\alias{set_batch_run_id_from_fsa}
\title{Set batch_run_id from fsa metadata}
\usage{
set_batch_run_id_from_fsa(fragments_list, config)
}
\arguments{
\item{fragments_list}{A list of fragments objects.}

\item{config}{A trace_config object (uses \code{batch_run_id_tag}).}
}
\value{
A trace_output status object (warnings for any mismatches). Modifies
the fragments objects in place.
}
\description{
Internal step that derives \code{batch_run_id} for fsa-imported samples directly
from the fsa file, so the fragment analysis run does not have to be entered
by hand. The fsa file is treated as authoritative: if a sample already has a
user-supplied \code{batch_run_id} that disagrees with the fsa-derived value, the
fsa value is used and a warning lists the mismatches so run mix-ups are
caught.
}
\keyword{internal}
