% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata_template.R
\name{generate_metadata_template}
\alias{generate_metadata_template}
\title{Generate a metadata template from fsa files}
\usage{
generate_metadata_template(input, output_csv = NULL)
}
\arguments{
\item{input}{One of: a list of fragments objects (from \code{\link[=read_fsa]{read_fsa()}}), a
character vector of \code{.fsa} file paths, or a single directory path
containing \code{.fsa} files (in which case the files are read with
\code{\link[=read_fsa]{read_fsa()}}).}

\item{output_csv}{Optional file path. If supplied, the template is written
to this csv with \code{write.csv()} (blank cells for values to be filled in).}
}
\value{
A data frame with one row per sample. The six columns recognised by
\code{\link[=add_metadata]{add_metadata()}} are included: \code{unique_id} and \code{batch_run_id} are
pre-filled from the fsa file, while \code{metrics_group_id},
\code{metrics_baseline_control}, \code{batch_sample_id}, and
\code{batch_sample_modal_repeat} are left blank (\code{NA}) for the user to
complete. Additional read-only \verb{fsa_*} provenance columns (run date, well,
plate, instrument, capillary, sample name) are included to help fill in
the rest; these are ignored by \code{\link[=add_metadata]{add_metadata()}}.
}
\description{
Create a metadata template with one row per sample and the \code{batch_run_id}
column already populated from each fsa file, ready to be filled in and
passed to \code{\link[=trace]{trace()}} (via \code{\link[=add_metadata]{add_metadata()}}).
}
\details{
This is the recommended starting point for building a metadata file.
Historically users typed the fragment analysis run for each sample by hand,
which is error prone. Here \code{batch_run_id} is read directly from the fsa file
(the \code{RunN} tag, or a date/time fallback) so samples are grouped by the run
they were actually processed in. Do not edit \code{batch_run_id} unless you know
a run was mislabelled. Rows are ordered by run then well so the template
reads like the original plate.
}
\examples{
fsa_list <- lapply(cell_line_fsa_list, function(x) x$clone())
# import data with read_fsa() to generate an equivalent list to cell_line_fsa_list

template <- generate_metadata_template(fsa_list)
head(template)

}
\seealso{
\code{\link[=read_fsa]{read_fsa()}}, \code{\link[=extract_fsa_metadata]{extract_fsa_metadata()}}, \code{\link[=add_metadata]{add_metadata()}}
}
