% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_report.R
\name{qc_report}
\alias{qc_report}
\title{Sample quality control report}
\usage{
qc_report(fragments_list, config_file = NULL, ...)
}
\arguments{
\item{fragments_list}{A list of fragments objects that have been processed
(e.g. with \code{\link[=trace]{trace()}}).}

\item{config_file}{Optional file path to a YAML config (see \code{\link[=trace]{trace()}}).}

\item{...}{QC threshold parameters that overwrite the config, namely
\code{qc_min_rsq}, \code{qc_min_peaks}, \code{qc_min_modal_signal},
\code{qc_saturation_ceiling}, \code{qc_window}, and \code{qc_prominence_min}.}
}
\value{
A data frame with one row per sample containing provenance
(\code{unique_id}, \code{batch_run_id}, \code{run_date}, \code{instrument}, \code{capillary},
\code{well}, \code{plate}), quality metrics (\code{ladder_avg_rsq}, \code{ladder_min_rsq},
\code{n_peaks}, \code{modal_size}, \code{modal_signal}, \code{modal_prominence},
\code{modal_saturated}, \code{saturation_in_window}), and a \code{qc_flags} column
(a \verb{;}-separated list of tripped checks) plus a logical \code{qc_pass}.
}
\description{
Summarise per-sample quality control metrics for a processed list of
fragments objects, flagging samples that may need attention before
calculating instability metrics.
}
\details{
No single metric identifies every problematic sample, so this is a panel of
checks and a sample fails if it trips any of them:
\itemize{
\item \code{low_ladder_rsq}: worst ladder segment R-squared (\code{ladder_min_rsq})
below \code{qc_min_rsq}. The worst segment is used rather than the average
because a single broken region can be hidden by a good average.
\item \code{few_peaks}: fewer than \code{qc_min_peaks} peaks detected (a failed or
empty sample).
\item \code{no_modal_peak}: no modal/allele peak could be identified
(\code{modal_size} or \code{modal_signal} is \code{NA}), regardless of \code{n_peaks}.
\item \code{low_signal}: modal peak signal below \code{qc_min_modal_signal}.
\item \code{saturated_modal}: the modal peak is off-scale or at/above
\code{qc_saturation_ceiling}, so peak heights are unreliable.
\item \code{saturation_in_window}: one or more off-scale peaks within
\code{qc_window} bp of the modal peak.
\item \code{low_prominence}: modal peak prominence (modal signal divided by
the mean of its immediate neighbours) below \code{qc_prominence_min}.
}

Note that modal peak prominence decreases as repeat length increases (long
repeats have broad distributions with no single dominant peak), so a
low-prominence flag is expected for very long repeats and should be
interpreted relative to the repeat size rather than as an absolute failure.
}
\examples{
fsa_list <- lapply(cell_line_fsa_list, function(x) x$clone())
# import data with read_fsa() to generate an equivalent list to cell_line_fsa_list

processed <- trace(fsa_list)
qc_report(processed)

}
\seealso{
\code{\link[=trace]{trace()}}, \code{\link[=extract_ladder_summary]{extract_ladder_summary()}}, \code{\link[=extract_fsa_metadata]{extract_fsa_metadata()}}
}
