% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fsa_metadata.R
\name{parse_fsa_metadata}
\alias{parse_fsa_metadata}
\title{Parse header fields from an ABIF (fsa) object}
\usage{
parse_fsa_metadata(abif)
}
\arguments{
\item{abif}{An ABIF object (the \code{fsa} field of a fragments object).}
}
\value{
A named list with elements: \code{run_id}, \code{run_name}, \code{run_datetime},
\code{instrument}, \code{instrument_serial}, \code{plate}, \code{well}, \code{capillary},
\code{sample_name}, \code{dye_names}, \code{n_offscale}, \code{n_saturated}.
}
\description{
Internal helper that extracts provenance and QC-relevant fields from the
ABIF directory returned by \code{\link[seqinr:read.abif]{seqinr::read.abif()}}. Missing tags yield \code{NA}
(or length-zero) rather than an error, so files from different instruments
are tolerated.
}
\details{
\code{run_id} is taken from the \code{RunN} tag (a ready-made unique run name, e.g.
"Run_MRSPHILLIPS2_2023-04-14_11-30_0063"). If \code{RunN} is absent it falls back
to a zero-padded "YYYYMMDD_HHMMSS" built from the \code{RUND}/\code{RUNT} tags.
}
\keyword{internal}
